class Settings < ApplicationRecord
    # enum speed: [:slow, :normal, :frantic]
    # enum hole_points: [:none, :one]
    # enum hole_spacing: [:close, :normal, :far_apart]
    # enum hole_size: [:small, :normal, :large]
  
    # belongs_to :game
  end